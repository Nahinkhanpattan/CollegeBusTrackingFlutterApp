Test Mode Rules - Copy and paste these into Firebase Console > Firestore Database > Rules:

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

⚠️ WARNING: These rules allow ANYONE to read/write all data. 
ONLY use for testing, then replace with proper rules immediately!

